package com.example.patten.adapter.before;

// 새로 도입해야 할 JSON 라이브러리 (인터페이스가 다름)
public class NewJsonLibrary {
    public void parseAndRunJson(String jsonData) { // 메서드 이름과 파라미터가 다름
        System.out.println("새로운 라이브러리로 JSON 데이터를 처리합니다: " + jsonData);
    }
}
